Read docs/system/system-state.md first.

Implement the first safe step toward household-scoped consistency across Money.

Requirements:
- preserve existing behavior where possible
- do not rename unrelated files
- do not change schema unless explicitly required
- flag assumptions clearly
- return reviewable diffs only

Output:
1. summary of changes
2. files changed
3. any follow-up work still needed